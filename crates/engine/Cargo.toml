[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
analyzers = { path = "../analyzers" }
cluster = { path = "../cluster" }
graph = { path = "../graph" }
kube = "3.0.1"
serde = { version = "1.0", features = ["derive"] }
types = { path = "../types" }
